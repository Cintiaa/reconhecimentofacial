import pyodbc
import pandas as pd

conn = pyodbc.connect(
    'Driver={SQL Server};'
    'Server=localhost;'
    'Database=reconhecimentoFacial;'
    'UID=sa;'
    'PWD=teste123;'
    'Trusted_Connection=yes;'
)
print(conn)

cursor = conn.cursor()
cursor.execute('SELECT * FROM Alunos')


for row in cursor:
    print('row = %r' % row)
conn.close()
